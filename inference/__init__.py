# Package docstring for RainPredictor-Inference
"""
rainpredictor_inference

Minimal inference-only package for the RainPredRNN radar nowcasting model.

This package provides:
- The model architecture (UNet encoder/decoder + temporal Transformer).
- Preprocessing utilities to build input tensors from radar TIFF images.
- A small helper to load checkpoints.
"""
